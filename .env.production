# Frontend producción (.env.production)
# URL pública del backend (sin barra final). Ajústala al dominio REAL de tu
# despliegue en Railway antes de publicar: este valor se hornea en el bundle.
# En Vercel puedes sobrescribirla con la variable de entorno VITE_API_BASE_URL.
VITE_API_BASE_URL=https://alcocars-api.up.railway.app
