# Producción: URL pública del backend (sin barra final). Ajústala al dominio
# REAL de tu despliegue en Railway antes de publicar: se hornea en el bundle.
# En Vercel puedes sobrescribirla con la variable de entorno del proyecto.
NEXT_PUBLIC_API_BASE_URL=https://alcocars-api.up.railway.app
