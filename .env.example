# Copia este archivo a .env y rellena los valores.
# Un solo archivo para todo: Next.js y el CLI de Prisma leen ambos .env.
# .env está en .gitignore: NUNCA subas credenciales al repositorio.

# === Base de datos (Supabase) ===
# Conexión con pooler (runtime) — puerto 6543. El pgbouncer y connection_limit=1
# son obligatorios en serverless: cada función abre su propia conexión.
DATABASE_URL="postgresql://postgres.<PROJECT-REF>:<PASSWORD>@aws-0-<region>.pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"
# Conexión directa (migraciones de Prisma) — puerto 5432, sin pooler
DIRECT_URL="postgresql://postgres.<PROJECT-REF>:<PASSWORD>@aws-0-<region>.pooler.supabase.com:5432/postgres"

# === Sesión del panel de administración ===
# Mínimo 32 caracteres en producción. Genera uno con: openssl rand -base64 48
JWT_SECRET=cambia-esto-por-una-cadena-aleatoria-larga-de-32-caracteres-minimo

# === Email (API de Brevo por HTTPS/443) ===
# 1. Crea una cuenta gratuita en https://www.brevo.com (300 emails/día)
# 2. Verifica un remitente en Senders & IPs → Senders
# 3. Crea una API Key en https://app.brevo.com/settings/keys/api
# Si BREVO_API_KEY queda vacío, el sistema NO rompe: guarda la reserva, registra
# el email como SKIPPED en email_logs y (en local) escribe una vista previa
# HTML en .mail-preview/ para revisar el diseño.
BREVO_API_KEY=
# Remitente verificado en Brevo. Formato: Nombre <email@dominio.es>
SMTP_FROM=Alcocars <reservas@alcocars.es>
# Buzón interno que recibe los avisos de reservas y de contacto
NOTIFY_EMAIL=ruviar@gmail.com
