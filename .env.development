# Frontend local development (.env.development)
# Option A (recommended): keep empty and let Vite proxy '/api' to http://localhost:3001.
# Option B: set an explicit backend URL, e.g. http://localhost:3001
VITE_API_BASE_URL=
